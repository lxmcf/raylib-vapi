# syntax=docker/dockerfile:1
FROM debian:sid
RUN set -eux && \
	apt-get update && \
	apt-get upgrade -y && \
	apt-get install -y meson valac libgl-dev libxcursor-dev libxrandr-dev \
	libxinerama-dev libxi-dev libsdl2-dev libdrm-dev libegl-dev libgbm-dev \
	libglfw3-dev
